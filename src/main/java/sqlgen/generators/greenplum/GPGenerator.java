package sqlgen.generators.greenplum;

import sqlgen.core.SQLFile;
import sqlgen.core.SQLGenerator;

import java.util.List;

public class GPGenerator implements SQLGenerator {
    @Override
    public List<SQLFile> generate() {
        return List.of();
    }
}
